[package]
name = "rescribe-read-biblatex"
version = "0.1.0"
edition = "2024"

[dependencies]
rescribe-core = { path = "../../rescribe-core" }
rescribe-std = { path = "../../nodes/rescribe-std" }
biblatex = "0.11"
