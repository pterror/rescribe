[package]
name = "rescribe-read-markdown-strict"
version = "0.1.0"
edition = "2024"

[dependencies]
rescribe-core = { path = "../../rescribe-core" }
rescribe-std = { path = "../../nodes/rescribe-std" }
pulldown-cmark = "0.12"
